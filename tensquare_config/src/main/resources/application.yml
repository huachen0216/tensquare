server:
  port: 12000
spring:
  application:
    name: tensquare-config
  cloud:
    config:
      server:
        git:
          uri: https://github.com/huachen0216/tensquare-config.git
  rabbitmq:
    host: 127.0.0.1
management:
  endpoints:
    web:
      exposure:
        include: bus-refresh